package model;

/**
 * Clase que representa una Planta de Proceso de salmón.
 * Hereda de UnidadOperativa e implementa Registrable.
 */
public class PlantaProceso extends UnidadOperativa implements Registrable {
    private int capacidadProceso;

    public int getCapacidadProceso() {
        return capacidadProceso;
    }

    public PlantaProceso(String nombre, String comuna, int capacidadProceso) {
        super(nombre, comuna);

        if (capacidadProceso <= 0) {
            throw new IllegalArgumentException("La capacidad de proceso debe ser positiva.");
        }

        this.capacidadProceso = capacidadProceso;
    }

    // Implementación de la Interfaz Registrable
    @Override
    public void registrar() {
        System.out.println("[REGISTRO] Planta de Proceso '" + nombre + "' registrada.");
    }

    @Override
    public void mostrarResumen() {
        System.out.println("Resumen de Planta: " + nombre + " en " + comuna + ". Capacidad: " + capacidadProceso + " tons.");
    }

    // Implementación polimórfica de UnidadOperativa
    @Override
    public void mostrarInformacion() {
        System.out.printf("| %-30s | %-15s | %,15d tons (Proceso) |\n", nombre, comuna, capacidadProceso);
    }
}
